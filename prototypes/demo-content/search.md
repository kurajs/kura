# 向量檢索原理

Kura 的檢索引擎是純 JavaScript、零相依,可以在 Node、Bun、Deno 與 Cloudflare Workers 上跑同一份程式碼。

語料小的時候用 f32 精確暴力搜尋,達到 100% 召回率;語料變大時自動切換到二值化(binary)預篩 +
f32 重新排序,在維持接近滿分召回率的同時,記憶體只需要原本的三十二分之一。查詢延遲大約每一千筆向量
零點一毫秒。
