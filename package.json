{
  "name": "kura-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Kura — agent-native documentation infrastructure, built on June.",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun run --filter \"*\" test",
    "check": "bash scripts/check-packages.sh"
  },
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.1",
    "publint": "^0.3.21"
  }
}
