# 給 AI Agent 使用

Kura 建立的知識庫開箱即是一個 MCP 伺服器。人類讀到的是 HTML 網頁,AI agent 則可以直接呼叫
`search_docs` 工具來查詢同一份內容,來源唯一、永不走樣。

每一篇文件同時提供三種投影:HTML 給人看、Markdown 原文與 JSON 資料給機器用。這讓你的產品文件
變成 agent 可以理解與操作的介面,而不只是給人看的內容。
